Compile and run the program on terminal in the project folder by giving the command:
make all

Clean the .out files by giving the command:
make clean

Install dependencies with the command:
sudo make install dependencies




 
